import React from "react";

const Modal = ({ setModal, setConfirm }) => {
  return (
    <div>

      {/* <!-- Put this part before </body> tag --> */}
      
    </div>
  );
};

export default Modal;
